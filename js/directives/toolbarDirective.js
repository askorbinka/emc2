(function () {
    var app = angular.module('logApp');
    
    app.directive('md-toolbar', function() {
        function link(scope, element, attrs) {
            
        }
        
        return {
            link: link
        }
    });
})();